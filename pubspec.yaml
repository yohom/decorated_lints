name: decorated_lints
description: A new Flutter package project.
version: 0.0.1
repository: https://github.com/yohom/decorated_lints

environment:
  sdk: ">=2.19.4 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  analyzer: ^5.7.0
  analyzer_plugin: ^0.11.2
  custom_lint_builder: ^0.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
